version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Making scripts executable..."
      - chmod +x scripts/*.sh
      - echo "Script permissions after chmod:"
      - ls -la scripts/
      - npm install
      - cd backend && npm install && cd ..
      - cd frontend && npm install && cd ..

  build:
    commands:
      - echo "Building frontend..."
      - cd frontend && npm run build && cd ..
      - echo "Build completed!"

artifacts:
  files:
    - '**/*'
  base-directory: .
